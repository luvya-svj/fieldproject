{
  "name": "medimap-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "npm run dev --prefix frontend",
    "server": "npm run server --prefix backend",
    "seed": "npm run seed --prefix backend",
    "seed:pg": "npm run seed:pg --prefix backend",
    "start": "npx concurrently \"npm run dev\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}